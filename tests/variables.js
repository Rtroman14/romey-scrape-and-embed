const { CHUNK_SIZE_TOKENS } = require("../src/variables");

console.log(`CHUNK_SIZE_TOKENS -->`, CHUNK_SIZE_TOKENS);
console.log(
    `CHUNK_SIZE_TOKENS * 0.1 + CHUNK_SIZE_TOKENS -->`,
    CHUNK_SIZE_TOKENS * 0.1 + CHUNK_SIZE_TOKENS
);
